<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- ConnectivityManager.requestNetwork() con WifiNetworkSpecifier lo exige
         en esta versión de Android, aunque no debería hacer falta según la
         documentación de la API — confirmado en hardware real 2026-09-21
         (SecurityException sin este permiso). Permiso normal: se otorga solo
         al instalar, sin diálogo al usuario (ver SentisNetworkManager.kt). -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:label="SENTIS Link Test"
        android:theme="@android:style/Theme.Material.Light">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
