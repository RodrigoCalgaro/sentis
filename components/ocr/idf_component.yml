## IDF Component Manager Manifest File — componente ocr
## Fase 2 (migración a app companion): ya no depende de pp_ocr_v6/esp-dl —
## la inferencia de OCR corre en el celular (ML Kit). Este componente solo
## captura+codifica JPEG (esp_driver_jpeg, sin dependencia externa) y usa
## components/link para el pedido/respuesta.
dependencies:
  idf: ">=5.3"
